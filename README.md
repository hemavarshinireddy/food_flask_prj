# food_flask_prj
this project include home, details, menu, bill generating pages done using python, CSS, html files (run the file in vs code and click the link for example http://127.0.0.1:5000 to view the web pages) 
